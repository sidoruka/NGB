sudo: required
os:
  - linux
dist: trusty
install: 
    - pip install mkdocs
    - pip install mkdocs-material
script: 
  - ./gradlew buildJar buildWar buildCli buildDoc buildDocker -PnoTest
after_success:
  - chmod 777 deploy.sh
  - ./deploy.sh
